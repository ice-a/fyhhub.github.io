# 构建环形链表
```js
function dispatchAction(action) {
  const update = {
    next: null,
    action
  }
  let pending
  if(!pending) {
    update.next = update
  } else {
    update.next = pending.next
    pending.next = update
  }
  pending = update
}
```